#! /bin/bash
# aosc-installer-prepare: helps generate aosc-installer-related information.
# Run this only once.

echo "sudo su aosc-installer-sh-interface" >> /etc/bashrc
add_favorite aosc-installer-qt

add_favourite() {
  # First test it if it exists
  # Adds the specified installer to various DEs' favourite list.
}